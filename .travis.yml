language: go

go:
  - 1.6
  - 1.5.3
  - tip

sudo: false

before_install:
  - go get github.com/golang/lint/golint

install: true

script:
  - go test -v $(go list ./... | grep -v /Godeps/)
  - go vet -x ./...
  - $(HOME)/gopath/bin/golint ./...
  - go build
